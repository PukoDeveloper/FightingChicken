import type { Core } from '../../core/Core.js';
import type { EnginePlugin } from '../../types/plugin.js';
/**
 * Plugin that manages **shop interactions**.
 *
 * Gold is read from / written to `VariableStoreManager` under the
 * `'player'` namespace (default), key `'gold'`.
 *
 * ### EventBus API
 *
 * | Event        | Params / Output                           |
 * |--------------|-------------------------------------------|
 * | `shop/define`| `ShopDefineParams`                        |
 * | `shop/open`  | `ShopOpenParams → ShopOpenOutput`         |
 * | `shop/buy`   | `ShopBuyParams → ShopBuyOutput`           |
 * | `shop/sell`  | `ShopSellParams → ShopSellOutput`         |
 * | `shop/close` | `ShopCloseParams`                         |
 */
export declare class ShopSystem implements EnginePlugin {
    readonly namespace = "shop";
    readonly dependencies: readonly ["inventory"];
    readonly editorMeta: {
        displayName: string;
        icon: string;
        description: string;
        events: readonly ["shop/define", "shop/open", "shop/close", "shop/buy", "shop/sell"];
    };
    private readonly _shops;
    private readonly _sessions;
    private readonly _goldNs;
    private readonly _goldKey;
    constructor(opts?: {
        goldNamespace?: string;
        goldKey?: string;
    });
    init(core: Core): void;
    destroy(core: Core): void;
    private _getGold;
    private _setGold;
    private _getItemDef;
}
//# sourceMappingURL=ShopSystem.d.ts.map
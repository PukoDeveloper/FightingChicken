import type { Core } from '../../core/Core.js';
import type { EnginePlugin } from '../../types/plugin.js';
/**
 * In-engine scene editor plugin.
 *
 * Provides a workflow for placing, moving, and removing scene objects driven
 * exclusively through the {@link EventBus}.
 *
 * ### EventBus API
 *
 * | Event                              | Params / Output                              |
 * |------------------------------------|----------------------------------------------|
 * | `sceneeditor/open`                 | `SceneEditorOpenParams`                      |
 * | `sceneeditor/close`                | `SceneEditorCloseParams`                     |
 * | `sceneeditor/tool:set`             | `SceneEditorToolSetParams`                   |
 * | `sceneeditor/actor-type:select`    | `SceneEditorActorTypeSelectParams`           |
 * | `sceneeditor/object:place`         | `SceneEditorObjectPlaceParams → Output`      |
 * | `sceneeditor/object:select`        | `SceneEditorObjectSelectParams`              |
 * | `sceneeditor/object:move`          | `SceneEditorObjectMoveParams`                |
 * | `sceneeditor/object:remove`        | `SceneEditorObjectRemoveParams`              |
 * | `sceneeditor/undo`                 | `SceneEditorUndoParams`                      |
 * | `sceneeditor/redo`                 | `SceneEditorRedoParams`                      |
 * | `sceneeditor/export`               | `{} → SceneEditorExportOutput`               |
 * | `sceneeditor/state`                | `{} → SceneEditorStateOutput`                |
 * | `sceneeditor/opened`               | `SceneEditorOpenedParams` (notification)     |
 * | `sceneeditor/closed`               | `SceneEditorClosedParams` (notification)     |
 * | `sceneeditor/objects:changed`      | `SceneEditorObjectsChangedParams` (notify)   |
 */
export declare class SceneEditorPlugin implements EnginePlugin {
    readonly namespace = "scene-editor";
    readonly dependencies: readonly ["input"];
    readonly editorMeta: {
        displayName: string;
        icon: string;
        description: string;
        events: readonly ["sceneeditor/open", "sceneeditor/close", "sceneeditor/tool:set", "sceneeditor/actor-type:select", "sceneeditor/object:place", "sceneeditor/object:select", "sceneeditor/object:move", "sceneeditor/object:remove", "sceneeditor/undo", "sceneeditor/redo", "sceneeditor/export", "sceneeditor/state"];
        schemas: {
            scene: {
                folder: string;
                displayName: string;
            };
        };
    };
    private _core;
    private _open;
    private _tool;
    private _selectedActorType;
    private _selectedObjectId;
    private _objects;
    private _undoStack;
    private _redoStack;
    private _idCounter;
    init(core: Core): void;
    destroy(core: Core): void;
    private readonly _onOpen;
    private readonly _onClose;
    private readonly _onToolSet;
    private readonly _onActorTypeSelect;
    private readonly _onObjectPlace;
    private readonly _onObjectSelect;
    private readonly _onObjectMove;
    private readonly _onObjectRemove;
    private readonly _onUndo;
    private readonly _onRedo;
    private readonly _onExport;
    private readonly _onState;
    private _getObjectsArray;
    private _pushCommand;
}
//# sourceMappingURL=SceneEditorPlugin.d.ts.map
import type { Core } from '../../core/Core.js';
import type { EnginePlugin } from '../../types/plugin.js';
/**
 * In-engine script node editor plugin.
 *
 * Provides a visual node-graph workflow for authoring scripts driven
 * exclusively through the {@link EventBus}.
 *
 * ### EventBus API
 *
 * | Event                               | Params / Output                              |
 * |-------------------------------------|----------------------------------------------|
 * | `scriptnodeeditor/open`             | `ScriptNodeEditorOpenParams`                 |
 * | `scriptnodeeditor/close`            | `ScriptNodeEditorCloseParams`                |
 * | `scriptnodeeditor/node:add`         | `ScriptNodeEditorNodeAddParams → Output`     |
 * | `scriptnodeeditor/node:update`      | `ScriptNodeEditorNodeUpdateParams`           |
 * | `scriptnodeeditor/node:remove`      | `ScriptNodeEditorNodeRemoveParams`           |
 * | `scriptnodeeditor/node:connect`     | `ScriptNodeEditorConnectParams`              |
 * | `scriptnodeeditor/undo`             | `ScriptNodeEditorUndoParams`                 |
 * | `scriptnodeeditor/redo`             | `ScriptNodeEditorRedoParams`                 |
 * | `scriptnodeeditor/export`           | `{} → ScriptNodeEditorExportOutput`          |
 * | `scriptnodeeditor/compile`          | `ScriptNodeEditorCompileParams`              |
 * | `scriptnodeeditor/state`            | `{} → ScriptNodeEditorStateOutput`           |
 * | `scriptnodeeditor/opened`           | `ScriptNodeEditorOpenedParams` (notify)      |
 * | `scriptnodeeditor/closed`           | `ScriptNodeEditorClosedParams` (notify)      |
 * | `scriptnodeeditor/nodes:changed`    | `ScriptNodeEditorNodesChangedParams` (notify)|
 */
export declare class ScriptNodeEditorPlugin implements EnginePlugin {
    readonly namespace = "script-node-editor";
    readonly dependencies: readonly ["script"];
    readonly editorMeta: {
        displayName: string;
        icon: string;
        description: string;
        events: readonly ["scriptnodeeditor/open", "scriptnodeeditor/close", "scriptnodeeditor/node:add", "scriptnodeeditor/node:update", "scriptnodeeditor/node:remove", "scriptnodeeditor/node:connect", "scriptnodeeditor/undo", "scriptnodeeditor/redo", "scriptnodeeditor/export", "scriptnodeeditor/compile", "scriptnodeeditor/state"];
        schemas: {
            script: {
                folder: string;
                displayName: string;
            };
        };
    };
    private _core;
    private _open;
    private _scriptId;
    private _nodes;
    private _undoStack;
    private _redoStack;
    private _idCounter;
    init(core: Core): void;
    destroy(core: Core): void;
    private readonly _onOpen;
    private readonly _onClose;
    private readonly _onNodeAdd;
    private readonly _onNodeUpdate;
    private readonly _onNodeRemove;
    private readonly _onNodeConnect;
    private readonly _onUndo;
    private readonly _onRedo;
    private readonly _onExport;
    private readonly _onCompile;
    private readonly _onState;
    private _compile;
    private _getNodesArray;
    private _pushCommand;
}
//# sourceMappingURL=ScriptNodeEditorPlugin.d.ts.map
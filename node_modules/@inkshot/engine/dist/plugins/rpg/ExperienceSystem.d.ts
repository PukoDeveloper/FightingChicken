import type { Core } from '../../core/Core.js';
import type { EnginePlugin } from '../../types/plugin.js';
/**
 * Plugin that manages **character level progression** via configurable
 * experience curves.
 *
 * ### EventBus API
 *
 * | Event              | Params / Output                          |
 * |--------------------|------------------------------------------|
 * | `exp/curve:define` | `ExpCurveDefineParams`                   |
 * | `exp/gain`         | `ExpGainParams → ExpGainOutput`          |
 * | `exp/set`          | `ExpSetParams`                           |
 * | `exp/get`          | `ExpGetParams → ExpGetOutput`            |
 * | `exp/levelup`      | `ExpLevelUpParams` (notification)        |
 * | `exp/gained`       | `ExpGainedParams` (notification)         |
 */
export declare class ExperienceSystem implements EnginePlugin {
    readonly namespace = "exp";
    readonly dependencies: readonly ["stats"];
    readonly editorMeta: {
        displayName: string;
        icon: string;
        description: string;
        events: readonly ["exp/curve:define", "exp/gain", "exp/get", "exp/set"];
    };
    private readonly _curves;
    private readonly _actorExp;
    init(core: Core): void;
    destroy(core: Core): void;
    /**
     * Returns total experience required to *reach* `level`.
     * Level 1 always requires 0 exp.
     */
    private _expForLevel;
}
//# sourceMappingURL=ExperienceSystem.d.ts.map
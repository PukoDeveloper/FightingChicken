import type { Core } from '../../core/Core.js';
import type { EnginePlugin } from '../../types/plugin.js';
/**
 * Plugin that manages **per-actor item inventories** and equipment.
 *
 * ### EventBus API
 *
 * | Event                   | Params / Output                                 |
 * |-------------------------|-------------------------------------------------|
 * | `inventory/item:define` | `InventoryItemDefineParams`                     |
 * | `inventory/add`         | `InventoryAddParams → InventoryAddOutput`       |
 * | `inventory/remove`      | `InventoryRemoveParams → InventoryRemoveOutput` |
 * | `inventory/use`         | `InventoryUseParams → InventoryUseOutput`       |
 * | `inventory/equip`       | `InventoryEquipParams → InventoryEquipOutput`   |
 * | `inventory/unequip`     | `InventoryUnequipParams → InventoryUnequipOutput`|
 * | `inventory/list`        | `InventoryListParams → InventoryListOutput`     |
 * | `inventory/has`         | `InventoryHasParams → InventoryHasOutput`       |
 */
export declare class InventorySystem implements EnginePlugin {
    readonly namespace = "inventory";
    readonly dependencies: readonly ["stats"];
    readonly editorMeta: {
        displayName: string;
        icon: string;
        description: string;
        events: readonly ["inventory/item:define", "inventory/item:get", "inventory/add", "inventory/remove", "inventory/use", "inventory/equip", "inventory/unequip", "inventory/list", "inventory/has"];
        schemas: {
            item: {
                folder: string;
                displayName: string;
            };
        };
    };
    private readonly _items;
    private readonly _bags;
    init(core: Core): void;
    destroy(core: Core): void;
    private _getOrCreateBag;
    private _defaultSlot;
}
//# sourceMappingURL=InventorySystem.d.ts.map